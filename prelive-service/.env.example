MONGO_URL=mongodb://localhost:27017
MONGO_DB_NAME=tradingai
# Must match backend/.env exactly if reading the encrypted broker token.
# If unset, the desk falls back to the plaintext dhan_config.py at D:/INDIAN MARKET.
BROKER_ENCRYPTION_KEY=
DHAN_CONFIG_DIR=D:/INDIAN MARKET
